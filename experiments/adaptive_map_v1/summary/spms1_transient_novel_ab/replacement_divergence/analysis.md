# 当前覆盖版 run02 核查

用户确认使用覆盖后的run02。原CSV SHA256=95c13c1fbcae6a74f26ce494155eba30a2372c82cf69eda334107e45ec2a4ccd。

重新执行NTU本地评估流程（棱镜转换、有效GT插值、全程SE(3)对齐），ATE RMSE=5.087780744m，3773行。结果保存在control_off/run02/evaluation_replacement_v1；原evaluation_v1仅保留历史记录，旧5.665712m不再代表当前输入。

## 分组限制

虽然目录为control_off，该次Transient novel_accepted最大50，novel_rejected合计454533，与保留配额行为一致。当前源码在Transient取消开关为false时不会增加该拒绝计数。因此不能将此次作为有效的取消配额重复，不自动重命名目录或重分类实验。没有启动参数/二进制快照，不能确定是启动漏参、旧程序还是其他运行版本差异。

当前目录下只有run01和run03仍符合取消配额行为，分别2.125791、0.544869m。不把三份目录文件直接求均值作为取消配额结果。

## 与run03的时间对照

为排除全程对齐把后期漂移分摊至起点的影响，诊断另以双方前60秒共用546个有效样本分别刚体对齐，全程共用有效时间戳2673个。以下误差仅为前段对齐诊断，不是全程ATE。

- 240～260秒共同有效样本48个，位置误差中位数run02=0.233m，run03=0.190m。
- 300～320秒共152个样本，run02=5.656m，run03=0.128m。
- 320～340秒run02=6.067m，run03=0.321m。
- 400秒后run02约17.871m，run03约1.664m。

260～300秒没有共同有效GT样本，无法给出这段内部的精确首次漂移时刻。绘图不跨真值缺口连接。

原始日志显示255～260秒：run02残差中位数0.193m，46个记录帧中44帧Transient，invalid_quality_rejected=90135，map_added=7941；run03分别0.076m、49帧中16帧、29790、20416。提示两次在这个窗口的匹配/地图更新行为已分化。

此后run02在300～320秒残差约0.06m，却已有约5.66m诊断位置偏差，说明小残差可能只是与当前地图保持局部一致，不能保证对真值正确。不能证明大量拒绝是初始原因，因为位姿先偏也会增加拒绝。

结论：本次不是旧5.666m的进一步分析，而是新的5.088m记录。值得检查的窗口前移至255秒附近；下一次实验应首先确认参数为false且日志行为确实取消配额，不能在当前分组不一致的情况下继续宣称取消配额的三次收益。未修改算法或baseline。

复现：`MPLCONFIGDIR=/tmp/adaptive_eval_mpl /home/romi/evo_env/bin/python scripts/audit_transient_novel_off.py --replacement`。
