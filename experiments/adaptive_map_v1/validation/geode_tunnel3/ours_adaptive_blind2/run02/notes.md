# Tunnel3 min_range=2m 重复运行run02

分别读取run01与run02，并在run02/evaluation_v1重新执行本地官方gamma2GT_leica.py及rmse.py。帧尾时间、SE(3)、max_diff=0.1s、offset=+0.1s作用于第二输入GT，与run01一致。

run02 ATE RMSE=0.150348m，与run01在评分输出的6位小数精度下一致。两组均2504行、adaptive_map全部为1，跨度252.000263s，最大CSV间隔1.799476s。

原始CSV哈希不同：
- run01：e82747149fce8ab78130db9d1da9af6b443602903db417be40ffafa2788dd288
- run02：a21cff5e801d25ed69abd65267ffb717cffe9868fdeb75327d50165a88cb48d0

逐行核验：帧首与帧尾时间戳完全一致；位置坐标分量最大绝对差约1.445e-6m；四元数分量最大差约1.895e-6；有效点数、模式、入图数、质量拒绝数、总拒绝数全部一致。因此不是误读同一路径，两个CSV也不是字节级相同文件。日志本身不能独立证明启动过程，但用户报告的重跑结果在本条件下高度一致。

结论：此前4.756198m的明显退步在这两次2米入图下限实验中均未出现。这支持配置距离限制是重要影响因素；相对关闭组0.146799m仍高约0.003549m，尚无明确模块增益。两次一致不等于跨场景统计稳定。

有效点20门槛相关的CSV空档仍存在，当前没有必要仅因这一空档立即修改门槛。原主矩阵未自动更换实验设置，参数对照结果保存在summary/tunnel3_min_range_comparison.csv。
